export enum Locale {
  NONE = 'none',
  KO = 'ko',
  EN = 'en',
  ENKO = 'enko',
}

export enum TranscribeEngine {
  CLOVA = 'CLOVA',
  RTZ = 'RTZ',
  HAIV = 'HAIV',
}

export enum Summarizer {
  OPENAI = 'OPENAI',
  SLLM = 'SLLM',
}
